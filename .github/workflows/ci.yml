name: CI/CD Pipeline Demo

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: 🔨 Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest

      - name: Run build
        run: |
          echo "🔨 Baue Anwendung..."
          python app.py || echo "Kein app.py vorhanden – Build-Simulation"

  test:
    name: 🧪 Test
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest

      - name: Run tests
        run: |
          echo "Starte Tests..."
          pytest -v || echo "Keine Tests gefunden – Test-Simulation"

  deploy:
    name: 🚀 Deploy (Simulation)
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Simulate deployment
        run: |
          echo "🚀 Deployment läuft..."
          echo "Bereitstellen der Anwendung aus Branch ${{ github.ref_name }}"
          echo "Deployment erfolgreich abgeschlossen ✅"
